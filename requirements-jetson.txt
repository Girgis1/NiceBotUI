# PIP requirements for NVIDIA Jetson (aarch64) deployments.
#
# OpenCV is provided by JetPack via apt (`python3-opencv`), so it is
# intentionally omitted to avoid building from source during setup.
PySide6>=6.5
numpy>=1.24
python-dateutil>=2.8
pytz>=2023.3
pyyaml>=6.0
psutil>=5.9.0
pyserial>=3.5
ultralytics>=8.0.0
lerobot @ git+https://github.com/huggingface/lerobot@main
